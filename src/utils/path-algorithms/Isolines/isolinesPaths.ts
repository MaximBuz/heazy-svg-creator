import { generateRandomNumber as random } from '../../helpers/randomNumber';
import { isolinePath } from './isolinePath';

export function isolinesPaths(
  seed: number,
  width: number,
  height: number,
  velocity: number,
  amount: number,
  depth: number
) {
  return;
}
